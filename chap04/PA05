#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main() {

	int h, r;

	printf("반지름의 길이? ");
	scanf("%d", &r);

	printf("높이? ");
	scanf("%d", &h);

	printf("원기둥의 부피: %lf", r * r * 3.14 * h);

	return 0;
}
