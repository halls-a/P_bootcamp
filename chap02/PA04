#include <stdio.h>

int main() {

	int h, m, s;

	printf("시? ");
	scanf_s("%d", &h);

	printf("분? ");
	scanf_s("%d", &m);

	printf("초? ");
	scanf_s("%d", &s);

	printf("입력한 시간은 %02d:%02d:%02d 입니다", h, m, s);

	return 0;
}
